[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = [
    "curl -LsSf https://astral.sh/uv/install.sh | sh",
    "/root/.local/bin/uv sync --frozen",
]

[start]
cmd = "/root/.local/bin/uv run uvicorn main:app --host 0.0.0.0 --port $PORT"
