
[phases.setup]
nixPkgs = ["python312", "gcc"]
cmds = ["curl -LsSf https://astral.sh/uv/install.sh | sh"]

[phases.install]
cmds = ["source $HOME/.cargo/env", "uv sync --frozen"]

[start]
cmd = "source $HOME/.cargo/env && uv run uvicorn app.main:app --host 0.0.0.0 --port $PORT"
