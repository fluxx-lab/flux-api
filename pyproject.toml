[project]
name = "flux-api"
version = "0.1.0"
description = "API for Fluxx lab"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.116.1",
    "python-dotenv>=1.1.1",
    "uvicorn[standard]>=0.35.0",
]
